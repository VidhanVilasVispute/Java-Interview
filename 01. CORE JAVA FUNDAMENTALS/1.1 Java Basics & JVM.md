What is Java and what are its key features?
Is Java 100% object-oriented? What are the advantages of Java being partially object-oriented?
What is the difference between primitive and non-primitive data types? Can primitive data types be null?
Can we declare pointers in Java?
What is the difference between JVM, JRE, and JDK?
What are the key components of JVM Architecture?
Can a Java application be run without installing the JRE?
Is it possible to have JDK without JRE?
What makes Java "write once, run anywhere"?
What is the Java Virtual Machine (JVM) and how does it work?
What are the types of memory areas allocated by JVM? Which is faster to access between heap and stack?
What is the role of the JIT (Just-In-Time) compiler?
What is a classloader in Java? How does class loading affect memory usage?
Is it possible to unload a class in Java?
How does garbage collection work in Java? What's the role of finalize() method?
Can you tell what algorithm JVM uses for garbage collection?
What is the difference between Young Generation and Old Generation memory spaces?
How can memory leaks occur in Java even with automatic garbage collection?
What is Java Flight Recorder (JFR)?
Which version of Java are you using?
